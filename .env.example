# Google Gemini API Key (Optional)
# If provided, the server will use this key for all users automatically.
# If omitted or invalid, users can connect their own key via the "Connect API" button in the header.
GEMINI_API_KEY=your_gemini_api_key_here

# Local Server Port (Defaults to 3000)
PORT=3000
